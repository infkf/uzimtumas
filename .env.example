# Database Configuration
# Choose: sqlite or mongodb
DATABASE_TYPE=sqlite

# MongoDB Atlas Configuration (only needed if DATABASE_TYPE=mongodb)
# Connection string from Atlas (with placeholders)
MONGODB_URI=mongodb+srv://<username>:<db_password>@cluster55533.oi333434gb.mongodb.net/?retryWrites=true&w=majority&appName=Cluster55533

# Your credentials (will replace placeholders in URI)
MONGODB_USERNAME=igor
MONGODB_PASSWORD=password

# Database name (separate from connection string)
MONGODB_DATABASE=gym_scraper